interface HomepageProps {
  setModule: () => void;
}

interface Module {
  setModule: () => void;
}

interface SortingModule extends Module {
  setModule: () => void;
}

interface ListsModule extends Module {
  setModule: () => void;
}
