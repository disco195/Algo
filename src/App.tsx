// Dependecies
import React, { Fragment } from "react";
import { Switch, Route } from "react-router-dom";

export default () => {
  /**
   * JSX
   */
  return (
    <Fragment>
      <Switch></Switch>
    </Fragment>
  );
};
